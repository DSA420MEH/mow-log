# Directives

Use this folder for human-readable feature directives. Keep files goal-focused and free of raw implementation code.

## Standard Flow
1. Create a directive from `feature-directive-template.md`.
2. Hand the directive to an execution agent.
3. Track completion and decisions in `session_log.md`.

## Rule
- A feature is not done until all acceptance criteria and verification checks are complete.
